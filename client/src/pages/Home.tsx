import React, {useEffect} from 'react';

const Home = () => {


    useEffect((): void => {
        // getStuffsAction(page)
    // }, [page]);
    }, []);

    useEffect(() => {
        return () => {
            // defStuffs()
        };
    }, []);

    return (
        <div>

        </div>
    );
};

export default Home;