{
  "scripts": {
    "docker:dev": "docker-compose -f docker-compose.dev.yml up -d",
    "docker:test": "docker-compose -f docker-compose.test.yml up -d",
    "docker:test-bd": "docker-compose -f docker-compose.test-bd.yml up -d",
    "docker:test-controllers": "docker-compose -f docker-compose.test-controllers.yml up -d"
  }
}
